dependencies:
  pre:
    - wget https://dl.bintray.com/mitchellh/packer/packer_0.8.1_linux_amd64.zip
    - unzip packer_0.8.1_linux_amd64.zip
    - rm -f packer_0.8.1_linux_amd64.zip
    - mv packer* /usr/local/bin

test:
  override:
    - packer build awsne-packer.json
